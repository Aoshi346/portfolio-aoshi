# Vice City Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implementar el tema Vice City del portfolio — hero en vídeo, composición y coreografía propias por sección, galería horizontal y créditos interactivos — según `docs/superpowers/specs/2026-07-24-vice-city-design.md`.

**Architecture:** El motor de temas ya existe y sortea un tema por visita. Vice añade tres cosas nuevas: un **backdrop de vídeo/foto** (en vez de shader), una **coreografía por sección** registrada por el tema (en vez de recetas genéricas por atributo), y **componentes interactivos** (galería, créditos). Un solo DOM semántico; la presentación la decide el CSS colgado de `[data-theme]`.

**Tech Stack:** Vite 8 · TypeScript (verbatimModuleSyntax) · Tailwind 4 (CSS-first `@theme`) · GSAP 3.15 + ScrollTrigger · Lenis · simple-icons · Playwright (verificación).

## Global Constraints

- **Node 22** obligatorio (`.nvmrc`). Con Node 18 el build de Vite 8 falla con `styleText`. Prefijo para todo comando: `export PATH="$HOME/.nvm/versions/node/v22.22.3/bin:$PATH"`.
- `npm run build` (tsc + vite) verde y `npm run lint` limpio = requisito de DONE de toda tarea.
- **Nunca `any`.** Usar `unknown` + guards. `verbatimModuleSyntax` activo: los imports de solo-tipo van con `import type`.
- `noUnusedLocals` y `noUnusedParameters` activos: no dejar variables ni parámetros sin usar.
- **Cero emojis** en código, commits y documentación.
- **Limpieza WebGL/GSAP obligatoria:** cancelar RAF, desconectar observers, `kill()` de ScrollTriggers y timelines, liberar recursos. Patrón de referencia: `src/backgrounds/shaderBackground.ts`.
- **`prefers-reduced-motion`**: sin vídeo, sin animación, sin letterbox, sin rodillo. La galería y el panel de créditos **siguen funcionando** (son contenido, no decoración).
- Contenido real en `src/data/content.ts`. Cero lorem/placeholder.
- Colores exactos de Vice: acento `#ffd166`, texto `#fff4e8`, fondo `#150726`.
- Tipografía de Vice: display `Passion One` 900, cuerpo/etiquetas `Manrope`. **Sin monoespaciada** (el mono es firma exclusiva de Hyprland).
- **No incrustar Pricedown.** Su licencia gratuita prohíbe `web page (embedded)`.
- Commits Conventional (`feat`, `fix`, `docs`, `refactor`, `chore`, `perf`).
- **No tocar el tema Hyprland.** Está aprobado; toda tarea debe verificar que no regresa.

---

## Estructura de archivos

**Crear**

| Archivo | Responsabilidad |
|---|---|
| `scripts/verify.py` | Arnés de verificación Playwright. Aserciones por tema; sale con código != 0 si falla |
| `src/backgrounds/cinematicBackdrop.ts` | Fondo de póster + vídeo + capas de gradación. Degradación por móvil/save-data/reduced-motion |
| `src/themes/choreography.ts` | Tipos del contrato de coreografía |
| `src/themes/vice.choreography.ts` | Coreografía por sección de Vice (GSAP) |
| `src/components/gallery.ts` | Galería horizontal arrastrable |
| `src/components/credits.ts` | Créditos interactivos (lista + panel de detalle) |
| `src/components/cinemaChrome.ts` | Letterbox y barra de orientación |
| `src/sections/contacto.ts` | Sección de cierre |

**Modificar:** `src/data/content.ts`, `src/themes/{types,vice,index}.ts`, `src/themes/themes.css`, `src/style.css`, `src/sections/{hero,about}.ts`, `src/sections/obra/projectScene.ts`, `src/utils/{reveal,icons}.ts`, `src/main.ts`, `index.html`.

**Retirar:** `src/three/heroScene.ts`, `src/backgrounds/viceSunset.ts`, `src/components/caseStudyPanel.ts`, `src/sections/caseStudies.ts`, y las dependencias `three` y `@types/three`.

**Reutilizar sin reescribir:** `src/utils/dom.ts` (`el`, `elFromMarkup`), `src/themes/index.ts` (`pickTheme`, `applyTheme`), `src/backgrounds/shaderBackground.ts` (Hyprland y Caelestia lo siguen usando).

### Contrato de atributos del DOM

La coreografía busca estos ganchos. Las secciones los emiten; la coreografía nunca usa selectores de clase de Tailwind.

| Atributo | Dónde |
|---|---|
| `data-scene="hero\|about\|obra\|credits\|contacto"` | En cada `<section>` |
| `data-hero-name` / `data-hero-fade` | Nombre del hero / elementos que se desvanecen antes del zoom |
| `data-card` / `data-line` / `data-stats` / `data-track` | Piezas de "Quién es" |
| `data-ord` / `data-title` / `data-meta` / `data-mask` | Piezas de la cartela de obra |
| `data-gallery` / `data-gallery-track` / `data-gallery-bar` | Galería |
| `data-credit` (con `data-index`) / `data-credit-roll` | Créditos |

---

## Task 1: Arnés de verificación y limpieza de la base

**Files:**
- Create: `scripts/verify.py`
- Modify: `package.json`
- Delete: `src/three/heroScene.ts`, `src/backgrounds/viceSunset.ts`

**Interfaces:**
- Consumes: nada.
- Produces: `python3 scripts/verify.py [--theme vice|hyprland|caelestia]` — arnés que todas las tareas siguientes extienden. Sale 0 si todas las aserciones pasan, 1 si alguna falla.

- [ ] **Step 1: Escribir el arnés con la primera aserción (que fallará)**

Crear `scripts/verify.py`:

```python
"""Arnes de verificacion visual. Cada tarea del plan anade aserciones aqui.

Uso: python3 scripts/verify.py [--theme vice] [--url http://127.0.0.1:5173]
Requiere el servidor de desarrollo levantado (npm run dev).
"""
import argparse
import sys
from playwright.sync_api import sync_playwright

CHROME = "/usr/bin/google-chrome"
ARGS = ["--no-sandbox", "--use-gl=swiftshader", "--enable-unsafe-swiftshader"]
DESKTOP = {"width": 1440, "height": 900}
MOBILE = {"width": 390, "height": 844}

failures: list[str] = []


def check(condition: bool, label: str) -> None:
    if condition:
        print(f"  OK   {label}")
    else:
        print(f"  FAIL {label}")
        failures.append(label)


def run(theme: str, url: str) -> None:
    with sync_playwright() as p:
        browser = p.chromium.launch(headless=True, executable_path=CHROME, args=ARGS)
        page = browser.new_page(viewport=DESKTOP)
        errors: list[str] = []
        page.on("console", lambda m: errors.append(m.text) if m.type == "error" else None)
        page.on("pageerror", lambda e: errors.append(f"pageerror: {e}"))

        page.goto(f"{url}/?theme={theme}", wait_until="domcontentloaded", timeout=30000)
        page.wait_for_timeout(3500)

        print(f"[{theme}] desktop")
        check(page.evaluate("document.documentElement.dataset.theme") == theme,
              "data-theme aplicado")
        check(not errors, f"cero errores de consola ({errors[:2]})")

        page.close()
        browser.close()


def main() -> int:
    ap = argparse.ArgumentParser()
    ap.add_argument("--theme", default="vice")
    ap.add_argument("--url", default="http://127.0.0.1:5173")
    args = ap.parse_args()
    run(args.theme, args.url)
    print()
    if failures:
        print(f"FALLOS: {len(failures)}")
        return 1
    print("TODO OK")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 2: Ejecutar y ver que pasa (línea base verde)**

```bash
export PATH="$HOME/.nvm/versions/node/v22.22.3/bin:$PATH"
npm run dev &
sleep 4
python3 scripts/verify.py --theme vice
```

Esperado: `TODO OK`. Esto confirma que el arnés funciona antes de usarlo para detectar fallos reales.

- [ ] **Step 3: Retirar el código que el spec deja obsoleto**

`src/backgrounds/viceSunset.ts` (el shader synthwave rechazado) y `src/three/heroScene.ts` (el icosaedro, ya sin importadores) salen. Verificar primero que nadie los importa:

```bash
grep -rn "viceSunset\|heroScene\|from \"three\"" src/ || echo "sin importadores"
```

Esperado: `sin importadores`.

```bash
git rm src/backgrounds/viceSunset.ts src/three/heroScene.ts
```

- [ ] **Step 4: Quitar las dependencias de Three.js**

En `package.json`, borrar la línea `"@types/three": "^0.185.1",` de `devDependencies` y `"three": "^0.185.1"` de `dependencies` (quitar también la coma sobrante de la línea previa).

```bash
export PATH="$HOME/.nvm/versions/node/v22.22.3/bin:$PATH"
npm install
```

- [ ] **Step 5: Verificar build, lint y que no hay regresión**

```bash
export PATH="$HOME/.nvm/versions/node/v22.22.3/bin:$PATH"
npm run build && npm run lint
python3 scripts/verify.py --theme hyprland
python3 scripts/verify.py --theme caelestia
```

Esperado: build verde, lint sin salida, y `TODO OK` en ambos temas. Vice quedará temporalmente sin fondo (su shader ya no existe); es esperado y lo resuelve la Task 3.

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "chore(vice): arnes de verificacion y retirada de three.js

Anade scripts/verify.py como arnes de aserciones Playwright.
Retira el shader synthwave de Vice (rechazado en diseno) y el icosaedro,
que ya no tenia importadores, junto con las dependencias three."
```

---

## Task 2: Modelo de datos

**Files:**
- Modify: `src/data/content.ts`

**Interfaces:**
- Produces:
  - `identity.availability: string`
  - `identity.since: string`
  - `interface GalleryShot { src: string; caption: string }`
  - `CaseStudy` gana: `lead: string`, `role: string`, `period?: string`, `status: string`, `gallery: GalleryShot[]`
  - `interface Stat { value: string; label: string }` y `export const stats: Stat[]`
  - `SkillItem` sin cambios (`name`, `slug`, `detail`) — el rol sale de `SkillGroup.label`

- [ ] **Step 1: Añadir la aserción que fallará**

En `scripts/verify.py`, dentro de `run()` justo antes de `page.close()`:

```python
        shape = page.evaluate("""(() => {
          const w = window;
          return w.__CONTENT_SHAPE__ || null;
        })()""")
        check(shape is not None and shape.get("galleries", 0) >= 2,
              "content.ts expone galerias en al menos 2 casos de estudio")
```

Y en `src/main.ts`, al final del archivo, exponer la forma solo para verificación:

```ts
// Sonda de verificacion: la consume scripts/verify.py. No afecta al render.
Object.defineProperty(window, "__CONTENT_SHAPE__", {
  value: { galleries: caseStudies.filter((project) => project.gallery.length > 0).length },
  writable: false,
});
```

- [ ] **Step 2: Ejecutar y ver que falla**

```bash
export PATH="$HOME/.nvm/versions/node/v22.22.3/bin:$PATH"
npm run build
```

Esperado: FALLA con `Property 'gallery' does not exist on type 'CaseStudy'`.

- [ ] **Step 3: Extender el modelo**

En `src/data/content.ts`, añadir a la interfaz `Identity` y a su constante:

```ts
export interface Identity {
  name: string;
  role: string;
  headline: string;
  subheadline: string;
  location: string;
  email: string;
  phone: string;
  github: string;
  githubAvatar: string;
  /** Estado visible en la ficha: la senal mas util para quien recluta. */
  availability: string;
  since: string;
}
```

Añadir a la constante `identity` (conservando los campos actuales):

```ts
  availability: "Abierto a oportunidades",
  since: "2021",
```

Añadir las cifras de la sección "Quién es":

```ts
export interface Stat {
  value: string;
  label: string;
}

/** Lectura instantanea antes de leer una sola frase. */
export const stats: Stat[] = [
  { value: "2021", label: "Desde" },
  { value: "9.º", label: "Semestre" },
  { value: "4", label: "Proyectos" },
  { value: "1", label: "En producción" },
];
```

Extender el caso de estudio:

```ts
export interface GalleryShot {
  /** Ruta bajo /public. Capturas reales del proyecto. */
  src: string;
  caption: string;
}

export interface CaseStudy {
  slug: string;
  title: string;
  tag: string;
  /** Linea de cartel: una frase, grande y ligera. */
  lead: string;
  role: string;
  period?: string;
  status: string;
  problem: string;
  solution: string;
  stack: string[];
  gallery: GalleryShot[];
  link?: { label: string; href: string };
  privateProject?: boolean;
}
```

Rellenar los cuatro casos existentes. Para `campaign-analytics`:

```ts
    lead: "Miles de campañas, un solo lugar donde entenderlas.",
    role: "Desarrollo integral",
    period: "Ago 2025 — hoy",
    status: "Privado de empresa",
    gallery: [
      { src: "/media/obra/campaign-panel.webp", caption: "Panel de campañas" },
      { src: "/media/obra/campaign-tabla.webp", caption: "Resultados por segmento" },
      { src: "/media/obra/campaign-segmentos.webp", caption: "Segmentación" },
    ],
```

Para `teg-web-app`:

```ts
    lead: "El ciclo completo del TEG, en un mismo lugar.",
    role: "Diseño y desarrollo",
    status: "Repositorio público",
    gallery: [
      { src: "/media/obra/teg-entregas.webp", caption: "Entregas de avances" },
      { src: "/media/obra/teg-jurados.webp", caption: "Asignación de jurados" },
    ],
```

Para `text-editor`:

```ts
    lead: "Un editor de texto que toca el sistema de verdad.",
    role: "Desarrollo en C",
    status: "Repositorio público",
    gallery: [{ src: "/media/obra/editor-ui.webp", caption: "Interfaz GTK4" }],
```

Para `ciberseg`:

```ts
    lead: "Las herramientas dispersas del día a día, en una sola suite.",
    role: "Diseño y desarrollo",
    status: "Repositorio público",
    gallery: [{ src: "/media/obra/ciberseg-suite.webp", caption: "Suite de escritorio" }],
```

- [ ] **Step 4: Verificar**

```bash
export PATH="$HOME/.nvm/versions/node/v22.22.3/bin:$PATH"
npm run build && npm run lint
python3 scripts/verify.py --theme vice
```

Esperado: build verde, lint limpio, y la aserción `content.ts expone galerias...` en OK.

> Las imágenes aún no existen; se añaden en la Task 10. La galería (Task 8) debe tolerar imágenes ausentes sin romper el layout.

- [ ] **Step 5: Commit**

```bash
git add src/data/content.ts src/main.ts scripts/verify.py
git commit -m "feat(content): lead, metadatos, galeria y cifras en el modelo

Anade a CaseStudy la linea de cartel, rol, periodo, estado y galeria de
capturas; a identity el estado de disponibilidad; y las cifras de la ficha."
```

---

## Task 3: Backdrop cinematográfico (póster + vídeo)

**Files:**
- Create: `src/backgrounds/cinematicBackdrop.ts`
- Modify: `src/themes/types.ts`, `src/themes/vice.ts`, `src/style.css`

**Interfaces:**
- Consumes: `BackgroundHandle` de `src/backgrounds/shaderBackground.ts`.
- Produces: `mountCinematicBackdrop(container: HTMLElement, options: BackdropOptions): BackgroundHandle` con `interface BackdropOptions { poster: string; video: { webm: string; mp4: string } }`.

- [ ] **Step 1: Añadir la aserción que fallará**

En `scripts/verify.py`, dentro de `run()`:

```python
        if theme == "vice":
            backdrop = page.evaluate("""(() => {
              const host = document.querySelector('.bg-theme');
              if (!host) return null;
              const img = host.querySelector('img');
              const video = host.querySelector('video');
              return {
                poster: !!img,
                video: !!video,
                playing: video ? !video.paused : false,
              };
            })()""")
            check(backdrop is not None and backdrop["poster"], "hay poster en el backdrop")
            check(backdrop is not None and backdrop["video"], "hay video en el backdrop")
            check(backdrop is not None and backdrop["playing"], "el video se reproduce")
```

- [ ] **Step 2: Ejecutar y ver que falla**

```bash
python3 scripts/verify.py --theme vice
```

Esperado: FAIL en las tres aserciones (Vice no tiene fondo desde la Task 1).

- [ ] **Step 3: Implementar el backdrop**

Crear `src/backgrounds/cinematicBackdrop.ts`:

```ts
import type { BackgroundHandle } from "./shaderBackground";

export interface BackdropOptions {
  /** Imagen que se pinta al instante; tambien es el fallback permanente. */
  poster: string;
  video: { webm: string; mp4: string };
}

/** El video es un lujo: solo se descarga si la conexion y el dispositivo lo permiten. */
function shouldLoadVideo(): boolean {
  if (window.matchMedia("(prefers-reduced-motion: reduce)").matches) return false;
  if (window.matchMedia("(max-width: 820px)").matches) return false;

  const nav = navigator as Navigator & { connection?: { saveData?: boolean } };
  if (nav.connection?.saveData) return false;

  return true;
}

/**
 * Fondo cinematografico: poster inmediato y, si procede, video en bucle que
 * entra fundido por encima. Las capas de gradacion, scrim y grano viven en CSS
 * (`.bg-theme` en style.css), asi que aqui solo se gestionan poster y video.
 */
export function mountCinematicBackdrop(
  container: HTMLElement,
  options: BackdropOptions,
): BackgroundHandle {
  const poster = document.createElement("img");
  poster.src = options.poster;
  poster.alt = "";
  poster.decoding = "async";
  poster.className = "backdrop-media";
  container.appendChild(poster);

  let video: HTMLVideoElement | null = null;

  if (shouldLoadVideo()) {
    video = document.createElement("video");
    video.muted = true;
    video.loop = true;
    video.autoplay = true;
    video.playsInline = true;
    video.preload = "auto";
    video.className = "backdrop-media backdrop-video";

    const webm = document.createElement("source");
    webm.src = options.video.webm;
    webm.type = "video/webm";
    const mp4 = document.createElement("source");
    mp4.src = options.video.mp4;
    mp4.type = "video/mp4";
    video.append(webm, mp4);

    // Solo se revela cuando hay fotogramas: nadie ve una caja negra.
    video.addEventListener("canplay", () => video?.classList.add("is-ready"), { once: true });
    container.appendChild(video);

    // Algunos navegadores rechazan el autoplay; el poster queda como fallback.
    void video.play().catch(() => undefined);
  }

  return {
    destroy() {
      if (video) {
        video.pause();
        video.removeAttribute("src");
        video.load();
        if (video.parentNode === container) container.removeChild(video);
      }
      if (poster.parentNode === container) container.removeChild(poster);
    },
  };
}
```

- [ ] **Step 4: Añadir el CSS de las capas**

En `src/style.css`, sustituir la regla `.bg-theme canvas` por:

```css
.bg-theme canvas,
.backdrop-media {
  position: absolute;
  inset: 0;
  display: block;
  width: 100%;
  height: 100%;
  object-fit: cover;
}

/* El video entra fundido sobre el poster cuando tiene fotogramas. */
.backdrop-video {
  opacity: 0;
  transition: opacity 900ms ease-out;
}

.backdrop-video.is-ready {
  opacity: 1;
}

/*
 * Capas de tratamiento del backdrop fotografico. El scrim es obligatorio:
 * garantiza contraste del texto pase lo que pase en la imagen.
 */
:root[data-theme="vice"] .bg-theme::before,
:root[data-theme="vice"] .bg-theme::after {
  content: "";
  position: absolute;
  inset: 0;
  pointer-events: none;
}

:root[data-theme="vice"] .bg-theme::before {
  background: linear-gradient(200deg, #ff2e88 0%, #7b1d6b 45%, #ffb347 100%);
  mix-blend-mode: soft-light;
  opacity: 0.48;
}

:root[data-theme="vice"] .bg-theme::after {
  background: linear-gradient(
    180deg,
    rgb(10 2 20 / 0.5) 0%,
    rgb(10 2 20 / 0.12) 45%,
    rgb(10 2 20 / 0.72) 100%
  );
}
```

- [ ] **Step 5: Conectar el tema Vice al backdrop**

En `src/themes/vice.ts`, sustituir `mountBackground` por:

```ts
  async mountBackground(container) {
    const { mountCinematicBackdrop } = await import("../backgrounds/cinematicBackdrop");
    return mountCinematicBackdrop(container, {
      poster: "/media/vice-poster.webp",
      video: { webm: "/media/vice-hero.webm", mp4: "/media/vice-hero.mp4" },
    });
  },
```

- [ ] **Step 6: Verificar**

Los archivos de `/media` aún no existen (Task 10). Para que esta tarea sea verificable, crear provisionalmente el póster desde la foto ya elegida:

```bash
mkdir -p public/media
curl -sL --max-time 60 -o public/media/vice-poster.webp \
  "https://images.unsplash.com/photo-1728324580682-d7e19f767f14?ixlib=rb-4.1.0&q=80&fm=webp&crop=entropy&cs=srgb&w=1920"
curl -sL --max-time 120 -o public/media/vice-hero.mp4 \
  "https://assets.mixkit.co/videos/4999/4999-720.mp4"
cp public/media/vice-hero.mp4 public/media/vice-hero.webm
export PATH="$HOME/.nvm/versions/node/v22.22.3/bin:$PATH"
npm run build && npm run lint
python3 scripts/verify.py --theme vice
```

Esperado: las tres aserciones del backdrop en OK.

> El `.webm` es una copia temporal del mp4 para desbloquear; la Task 10 lo sustituye por un WebM real optimizado.

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "feat(vice): backdrop cinematografico con poster y video en bucle

El video solo se descarga si no hay reduced-motion, no es movil y no hay
save-data. El poster se pinta al instante y queda como fallback permanente."
```

---

## Task 4: Tokens y tipografía de Vice

**Files:**
- Modify: `src/themes/themes.css`, `src/themes/vice.ts`, `src/style.css`

**Interfaces:**
- Produces: tokens `--font-lead`, `--lead-weight`, `--lead-size` y clase `.lead`.

- [ ] **Step 1: Añadir la aserción que fallará**

En `scripts/verify.py`, en el bloque `if theme == "vice":`:

```python
            fonts = page.evaluate("""(() => {
              const root = getComputedStyle(document.documentElement);
              const probe = document.querySelector('[data-hero-name]');
              return {
                display: root.getPropertyValue('--font-display').trim(),
                body: root.getPropertyValue('--font-body').trim(),
                accent: root.getPropertyValue('--color-accent').trim(),
                rendered: probe ? getComputedStyle(probe).fontFamily : "",
              };
            })()""")
            check("Passion One" in fonts["display"], "display es Passion One")
            check("Manrope" in fonts["body"], "cuerpo es Manrope")
            check(fonts["accent"].lower() == "#ffd166", "acento es ambar")
            check("mono" not in fonts["body"].lower(), "Vice no usa monoespaciada")
```

- [ ] **Step 2: Ejecutar y ver que falla**

```bash
python3 scripts/verify.py --theme vice
```

Esperado: FAIL en display, cuerpo y acento (hoy son Anton/Inter/`#ff2e88`).

- [ ] **Step 3: Actualizar el bloque de tokens de Vice**

En `src/themes/themes.css`, sustituir el bloque `:root[data-theme="vice"]` por:

```css
:root[data-theme="vice"] {
  --color-ink: #150726;
  --color-paper: #fff4e8;
  --color-accent: #ffd166;
  --color-accent-2: #ff2e88;
  --color-line: rgb(255 209 102 / 0.32);

  --font-display: "Passion One", system-ui, sans-serif;
  --font-body: "Manrope", system-ui, sans-serif;
  --font-lead: "Manrope", system-ui, sans-serif;

  --radius-card: 3px;
  --display-tracking: 0.005em;
  --display-weight: 900;
  --display-transform: uppercase;
  --display-leading: 0.84;

  /* La linea de cartel: grande y ultraligera, nunca un parrafo pequeno. */
  --lead-weight: 200;
  --lead-size: clamp(1.05rem, 2vw, 1.45rem);

  --bg-fallback: linear-gradient(180deg, #150726 0%, #4b1146 55%, #ff5c3d 100%);
}
```

- [ ] **Step 4: Cargar las tipografías del tema**

En `src/themes/vice.ts`, sustituir `fontHref` por:

```ts
  // Passion One 900: alternativa libre mas cercana a Pricedown, cuya licencia
  // gratuita prohibe incrustarla como webfont. Manrope cubre lead y cuerpo.
  fontHref:
    "https://fonts.googleapis.com/css2?family=Passion+One:wght@900&family=Manrope:wght@200;300;400;600;700&display=swap",
```

- [ ] **Step 5: Añadir la clase de subtítulo**

En `src/style.css`, tras el bloque `.display-xl, .display-lg`:

```css
/*
 * Subtitulo. Es una linea de cartel, no un parrafo: por eso tiene su propio
 * token de familia, peso y tamano. Con 13,5 px y opacidad baja pesaba menos
 * que el cuerpo y la jerarquia se rompia.
 */
.lead {
  font-family: var(--font-lead, var(--font-body));
  font-weight: var(--lead-weight, 300);
  font-size: var(--lead-size, 1.125rem);
  line-height: 1.4;
}
```

- [ ] **Step 6: Verificar que Hyprland no regresa**

```bash
export PATH="$HOME/.nvm/versions/node/v22.22.3/bin:$PATH"
npm run build && npm run lint
python3 scripts/verify.py --theme vice
python3 scripts/verify.py --theme hyprland
```

Esperado: aserciones de tipografía en OK y Hyprland en `TODO OK`.

- [ ] **Step 7: Commit**

```bash
git add src/themes/themes.css src/themes/vice.ts src/style.css scripts/verify.py
git commit -m "feat(vice): tokens ambar y pareja Passion One + Manrope

Retira la monoespaciada del tema (firma exclusiva de Hyprland) y anade los
tokens del subtitulo, que pasa a ser una linea de cartel con peso propio."
```

---

## Task 5: Motor de coreografía por sección

**Files:**
- Create: `src/themes/choreography.ts`
- Modify: `src/themes/types.ts`, `src/utils/reveal.ts`, `src/themes/vice.ts`

**Interfaces:**
- Produces:
  - `interface ChoreographyContext { gsap: Gsap; ScrollTrigger: ScrollTriggerApi; root: HTMLElement; motion: MotionProfile }`
  - `type Choreography = (context: ChoreographyContext) => void`
  - `Theme.choreography?: () => Promise<Choreography>`
  - `initScrollReveal(root, theme)` pasa a recibir el tema completo, no solo el perfil de motion.

- [ ] **Step 1: Definir el contrato**

Crear `src/themes/choreography.ts`:

```ts
import type { MotionProfile } from "./types";

export type Gsap = typeof import("gsap").default;
export type ScrollTriggerApi = typeof import("gsap/ScrollTrigger").ScrollTrigger;

export interface ChoreographyContext {
  gsap: Gsap;
  ScrollTrigger: ScrollTriggerApi;
  /** Raiz sobre la que buscar los ganchos `data-*`. */
  root: HTMLElement;
  motion: MotionProfile;
}

/**
 * Coreografia completa de un tema. Sustituye a las recetas genericas por
 * atributo: un gesto propio por seccion, no el mismo fade en todas partes.
 */
export type Choreography = (context: ChoreographyContext) => void;
```

- [ ] **Step 2: Extender el tipo Theme**

En `src/themes/types.ts`, añadir el import y el campo:

```ts
import type { Choreography } from "./choreography";
```

Dentro de `interface Theme`, tras `motion`:

```ts
  /**
   * Coreografia propia del tema, cargada en diferido. Si no la define, se
   * aplican las recetas genericas de `utils/reveal.ts`.
   */
  choreography?: () => Promise<Choreography>;
```

- [ ] **Step 3: Delegar desde reveal.ts**

En `src/utils/reveal.ts`, cambiar la firma y el arranque. Sustituir la cabecera de `initScrollReveal` por:

```ts
export async function initScrollReveal(root: HTMLElement, theme: Theme): Promise<void> {
  const motion = theme.motion;
  const prefersReducedMotion = window.matchMedia("(prefers-reduced-motion: reduce)").matches;
  if (prefersReducedMotion) return;

  const [{ default: gsap }, { ScrollTrigger }] = await Promise.all([
    import("gsap"),
    import("gsap/ScrollTrigger"),
  ]);
  gsap.registerPlugin(ScrollTrigger);

  if (motion.style === "cinematic") await initSmoothScroll(gsap, ScrollTrigger);

  // Un tema con coreografia propia la usa en exclusiva: mezclarla con las
  // recetas genericas haria que dos timelines peleen por el mismo elemento.
  if (theme.choreography) {
    const choreography = await theme.choreography();
    choreography({ gsap, ScrollTrigger, root, motion });
    ScrollTrigger.refresh();
    return;
  }

  // ... (a partir de aqui, el cuerpo generico existente queda intacto)
```

Añadir el import de tipo al principio del archivo:

```ts
import type { Theme } from "../themes/types";
```

Y borrar las llamadas a `playCinematicIntro` y `wireHeroZoom` del cuerpo genérico junto con esas dos funciones: pasan a la coreografía de Vice en la Task 6.

- [ ] **Step 4: Actualizar la llamada en main.ts**

En `src/main.ts`, cambiar la última línea:

```ts
void import("./utils/reveal").then(({ initScrollReveal }) => initScrollReveal(main, theme));
```

- [ ] **Step 5: Registrar una coreografía vacía en Vice**

En `src/themes/vice.ts`, añadir al objeto:

```ts
  async choreography() {
    const { viceChoreography } = await import("./vice.choreography");
    return viceChoreography;
  },
```

Y crear `src/themes/vice.choreography.ts` con el esqueleto que las tareas siguientes rellenan:

```ts
import type { Choreography } from "./choreography";

/** Coreografia de Vice: un gesto por seccion, en lenguaje de cine. */
export const viceChoreography: Choreography = () => {
  // Las secciones se van cableando en las tareas 6 a 9.
};
```

- [ ] **Step 6: Verificar**

```bash
export PATH="$HOME/.nvm/versions/node/v22.22.3/bin:$PATH"
npm run build && npm run lint
python3 scripts/verify.py --theme vice
python3 scripts/verify.py --theme hyprland
```

Esperado: build verde, lint limpio, cero errores de consola en ambos. Vice queda sin animaciones (coreografía vacía); Hyprland conserva las suyas.

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "refactor(motion): coreografia por tema en lugar de recetas globales

Los temas pueden registrar una coreografia propia, cargada en diferido. Los
que no la definen siguen con las recetas genericas por atributo."
```

---

## Task 6: Hero — composición y gesto de título

**Files:**
- Modify: `src/sections/hero.ts`, `src/themes/vice.choreography.ts`, `src/themes/themes.css`, `src/style.css`

**Interfaces:**
- Consumes: `Choreography` de la Task 5.
- Produces: `<section data-scene="hero">` con `[data-hero-name]`, `[data-hero-fade]`.

- [ ] **Step 1: Añadir la aserción que fallará**

En `scripts/verify.py`, en el bloque de Vice:

```python
            hero = page.evaluate("""(() => {
              const s = document.querySelector('[data-scene="hero"]');
              if (!s) return null;
              const name = s.querySelector('[data-hero-name]');
              const mail = document.querySelector('a[href^="mailto:"]');
              const r = mail ? mail.getBoundingClientRect() : null;
              return {
                name: !!name,
                chars: name ? name.querySelectorAll('span span').length : 0,
                mailVisible: !!(r && r.top >= 0 && r.top < window.innerHeight),
              };
            })()""")
            check(hero is not None and hero["name"], "el hero tiene nombre marcado")
            check(hero is not None and hero["chars"] > 5, "el nombre se parte en caracteres")
            check(hero is not None and hero["mailVisible"],
                  "el email es visible en el primer viewport")
```

- [ ] **Step 2: Ejecutar y ver que falla**

```bash
python3 scripts/verify.py --theme vice
```

Esperado: FAIL en `chars` (la coreografía está vacía y ya no parte el texto).

- [ ] **Step 3: Reescribir el hero**

Sustituir el contenido de `src/sections/hero.ts` por:

```ts
import { identity } from "../data/content";
import { el } from "../utils/dom";

/**
 * Apertura. Suelo de conversion garantizado: nombre, rol y contacto legibles
 * al instante, sin depender de que el video o las animaciones hayan cargado.
 * El layout concreto lo decide el tema (themes.css).
 */
export function createHero(): HTMLElement {
  const eyebrow = el("p", "hero-kick", [identity.role]);
  eyebrow.setAttribute("data-hero-fade", "");

  const name = el("h1", "display-xl mt-4 text-[clamp(2.8rem,11vw,9.5rem)]", [identity.name]);
  name.setAttribute("data-hero-name", "");

  const lead = el("p", "lead mx-auto mt-5 max-w-[32ch] text-paper/85", [identity.subheadline]);
  lead.setAttribute("data-hero-fade", "");

  const location = el("span", "", [identity.location]);

  const email = el("a", "hero-mail", [identity.email]);
  email.href = `mailto:${identity.email}`;

  const corner = el("div", "hero-corner", [location, email]);
  corner.setAttribute("data-hero-fade", "");

  const section = el(
    "section",
    "hero relative flex min-h-screen flex-col justify-center overflow-hidden px-6 py-24 md:px-12",
    [eyebrow, name, lead, corner],
  );
  section.setAttribute("data-scene", "hero");

  return section;
}
```

- [ ] **Step 4: Añadir el CSS del hero**

En `src/style.css`:

```css
.hero-kick {
  font-weight: 700;
  font-size: 0.66rem;
  letter-spacing: 0.24em;
  text-transform: uppercase;
  color: var(--color-accent);
}

.hero-corner {
  position: absolute;
  bottom: 9%;
  left: 1.5rem;
  right: 1.5rem;
  display: flex;
  justify-content: space-between;
  gap: 1rem;
  font-weight: 700;
  font-size: 0.6rem;
  letter-spacing: 0.2em;
  text-transform: uppercase;
  color: rgb(255 244 232 / 0.6);
}

@media (min-width: 768px) {
  .hero-corner {
    left: 3rem;
    right: 3rem;
  }
}

.hero-mail {
  color: var(--color-accent);
  text-decoration: underline;
  text-underline-offset: 7px;
  text-decoration-thickness: 2px;
}
```

En `src/themes/themes.css`, el centrado propio de Vice:

```css
/* Cartel centrado: el nombre manda y luego se atraviesa al hacer scroll. */
:root[data-theme="vice"] .hero {
  align-items: center;
  text-align: center;
}
```

- [ ] **Step 5: Escribir el gesto de título**

Sustituir `src/themes/vice.choreography.ts` por:

```ts
import type { Choreography, Gsap } from "./choreography";

/**
 * Parte el texto en spans por caracter, agrupados por palabra para que la
 * linea siga rompiendo donde toca. A mano en vez de con SplitText para no
 * atar el redisenio a la disponibilidad del plugin.
 */
function splitChars(target: HTMLElement): HTMLElement[] {
  const text = target.textContent ?? "";
  target.textContent = "";
  const chars: HTMLElement[] = [];
  const words = text.split(" ");

  words.forEach((word, index) => {
    const wordSpan = document.createElement("span");
    wordSpan.className = "inline-block overflow-hidden whitespace-nowrap align-bottom";
    for (const character of word) {
      const charSpan = document.createElement("span");
      charSpan.className = "inline-block";
      charSpan.textContent = character;
      wordSpan.append(charSpan);
      chars.push(charSpan);
    }
    target.append(wordSpan);
    if (index < words.length - 1) target.append(document.createTextNode(" "));
  });

  return chars;
}

/** Gesto 1 — Titulo de apertura: el nombre se monta y luego te atraviesa. */
function scene1Title(gsap: Gsap, root: HTMLElement): void {
  const hero = root.querySelector<HTMLElement>('[data-scene="hero"]');
  const name = root.querySelector<HTMLElement>("[data-hero-name]");
  if (!hero || !name) return;

  const chars = splitChars(name);
  const fading = Array.from(root.querySelectorAll<HTMLElement>("[data-hero-fade]"));

  gsap.set(chars, { yPercent: 115 });
  gsap.set(name, { scale: 1.08, filter: "blur(7px)" });
  gsap.set(fading, { opacity: 0, y: 24 });
  document.documentElement.classList.remove("js-intro");

  const intro = gsap.timeline();
  // Empuje de camara: lento y sostenido, da el aire de plano de apertura.
  intro.to(name, { scale: 1, filter: "blur(0px)", duration: 2.4, ease: "power2.out" }, 0);
  // La duracion percibida la manda el stagger, no el easing: con expo.out el
  // 96% del recorrido se gastaba en 250 ms y la entrada se leia instantanea.
  intro.to(chars, { yPercent: 0, duration: 1.0, ease: "power3.out", stagger: 0.075 }, 0.25);
  intro.to(fading, { opacity: 1, y: 0, duration: 0.9, ease: "power2.out", stagger: 0.14 }, 1.5);

  const exit = gsap.timeline({
    scrollTrigger: { trigger: hero, start: "top top", end: "+=150%", pin: true, scrub: 1 },
  });
  // El acompanamiento se va primero: si todo se arrastra a la vez, se ve roto.
  exit.to(fading, { opacity: 0, y: -30, ease: "none", duration: 0.28 }, 0);
  exit.to(name, { scale: 9, opacity: 0, ease: "power2.in", duration: 1 }, 0.12);
}

export const viceChoreography: Choreography = ({ gsap, root }) => {
  scene1Title(gsap, root);
};
```

- [ ] **Step 6: Verificar**

```bash
export PATH="$HOME/.nvm/versions/node/v22.22.3/bin:$PATH"
npm run build && npm run lint
python3 scripts/verify.py --theme vice
```

Esperado: las tres aserciones del hero en OK.

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "feat(vice): hero centrado con gesto de titulo de apertura

El acompanamiento se desvanece antes de que el nombre crezca; si todo se
arrastra a la vez el gesto se lee roto."
```

---

## Task 7: Quién es — ficha, cifras y gesto de subtitulado

**Files:**
- Modify: `src/sections/about.ts`, `src/themes/vice.choreography.ts`, `src/style.css`

**Interfaces:**
- Produces: `<section data-scene="about">` con `[data-card]`, `[data-line]`, `[data-stats]`, `[data-track]`.

- [ ] **Step 1: Añadir la aserción que fallará**

```python
            about = page.evaluate("""(() => {
              const s = document.querySelector('[data-scene="about"]');
              if (!s) return null;
              return {
                card: !!s.querySelector('[data-card]'),
                avatar: !!s.querySelector('[data-card] img'),
                stats: s.querySelectorAll('[data-stats] > *').length,
                track: !!s.querySelector('[data-track]'),
                chips: s.querySelectorAll('[data-tech-chip]').length,
              };
            })()""")
            check(about is not None and about["card"], "hay ficha de reparto")
            check(about is not None and about["avatar"], "la ficha lleva avatar")
            check(about is not None and about["stats"] == 4, "hay cuatro cifras")
            check(about is not None and about["track"], "hay trayectoria")
            check(about is not None and about["chips"] == 0,
                  "sin chips de tecnologia (duplicarian los creditos)")
```

- [ ] **Step 2: Ejecutar y ver que falla**

```bash
python3 scripts/verify.py --theme vice
```

Esperado: FAIL en ficha, avatar, cifras y trayectoria.

- [ ] **Step 3: Reescribir la sección**

Sustituir `src/sections/about.ts` por:

```ts
import { education, experience, identity, stats } from "../data/content";
import { el } from "../utils/dom";

function createCard(): HTMLElement {
  const avatar = el("img", "about-avatar");
  avatar.src = identity.githubAvatar;
  avatar.alt = identity.name;
  avatar.width = 50;
  avatar.height = 50;
  avatar.loading = "lazy";
  avatar.decoding = "async";

  const status = el("p", "about-status", [
    el("span", "about-dot", []),
    identity.availability,
  ]);

  const facts = el("dl", "about-facts", [
    el("dt", "", ["Rol"]),
    el("dd", "", [identity.role]),
    el("dt", "", ["Base"]),
    el("dd", "", [identity.location]),
    el("dt", "", ["Ahora"]),
    el("dd", "", [experience[0]?.organization ?? ""]),
    el("dt", "", ["Estudia"]),
    el("dd", "", [education[0]?.institution ?? ""]),
  ]);

  const card = el("div", "about-card", [
    avatar,
    el("p", "about-name display-lg text-2xl", [identity.name]),
    status,
    facts,
  ]);
  card.setAttribute("data-card", "");
  return card;
}

function createStats(): HTMLElement {
  const group = el(
    "div",
    "about-stats",
    stats.map((stat) =>
      el("div", "", [el("b", "", [stat.value]), el("span", "", [stat.label])]),
    ),
  );
  group.setAttribute("data-stats", "");
  return group;
}

function createTrack(): HTMLElement {
  const path = el("div", "", [
    el("h3", "about-h", ["Trayectoria"]),
    ...experience.map((item) =>
      el("div", "about-item", [
        el("b", "", [`${item.role} · ${item.organization}`]),
        el("span", "", [item.period]),
      ]),
    ),
    ...education.map((item) =>
      el("div", "about-item", [
        el("b", "", [item.degree]),
        el("span", "", [`${item.institution} · ${item.period}`]),
      ]),
    ),
  ]);

  const focus = el("div", "", [
    el("h3", "about-h", ["En qué me enfoco"]),
    el("div", "about-item", [
      el("b", "", ["Datos a gran escala"]),
      el("span", "", ["Que la consulta siga siendo rápida con volumen real"]),
    ]),
    el("div", "about-item", [
      el("b", "", ["Interfaces que aguantan"]),
      el("span", "", ["Estado complejo sin romperse en producción"]),
    ]),
  ]);

  const track = el("div", "about-track", [path, focus]);
  track.setAttribute("data-track", "");
  return track;
}

/** Linea envuelta en su mascara: sube desde detras al entrar. */
function createLine(className: string, text: string): HTMLElement {
  const line = el("span", "about-line", [el("span", className, [text])]);
  line.setAttribute("data-line", "");
  return line;
}

export function createAbout(): HTMLElement {
  const body = el("div", "about-body", [
    createLine("lead text-paper/90", "Llevo datos reales a interfaces que la gente usa todos los días."),
    createLine(
      "block mt-3 text-sm leading-relaxed text-paper/85",
      "La mayoría de mis repositorios son privados. Aquí están los públicos que mejor muestran cómo pienso y qué construyo.",
    ),
    createStats(),
    createTrack(),
  ]);

  const section = el(
    "section",
    "about relative flex min-h-screen flex-col justify-center px-6 py-24 md:px-12",
    [
      el("p", "hero-kick", ["Quién es"]),
      el("div", "about-grid", [createCard(), body]),
    ],
  );
  section.setAttribute("data-scene", "about");
  return section;
}
```

- [ ] **Step 4: Añadir el CSS**

En `src/style.css`:

```css
.about-grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: clamp(1.4rem, 4vw, 3.4rem);
  align-items: start;
  margin-top: 1rem;
}

@media (min-width: 860px) {
  .about-grid {
    grid-template-columns: minmax(190px, 235px) 1fr;
  }
}

.about-card {
  border: 1px solid var(--color-line);
  border-radius: var(--radius-card);
  padding: 1rem 1.05rem;
  background: rgb(10 2 20 / 0.38);
  backdrop-filter: blur(6px);
}

.about-avatar {
  width: 50px;
  height: 50px;
  border-radius: 50%;
  object-fit: cover;
  border: 1px solid var(--color-line);
}

.about-name {
  margin-top: 0.6rem;
}

.about-status {
  display: flex;
  align-items: center;
  gap: 0.4rem;
  margin-top: 0.5rem;
  font-weight: 600;
  font-size: 0.62rem;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: #8ff0b5;
}

.about-dot {
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: #6ee7a0;
}

/* El pulso solo si el usuario acepta movimiento. */
@media (prefers-reduced-motion: no-preference) {
  .about-dot {
    animation: about-blip 2s infinite;
  }
}

@keyframes about-blip {
  0% { box-shadow: 0 0 0 0 rgb(110 231 160 / 0.5); }
  70% { box-shadow: 0 0 0 7px rgb(110 231 160 / 0); }
  100% { box-shadow: 0 0 0 0 rgb(110 231 160 / 0); }
}

.about-facts {
  margin-top: 0.75rem;
  display: grid;
  grid-template-columns: auto 1fr;
  gap: 0.4rem 0.7rem;
}

.about-facts dt {
  font-weight: 700;
  font-size: 0.53rem;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: var(--color-accent);
  padding-top: 2px;
}

.about-facts dd {
  margin: 0;
  font-size: 0.7rem;
  line-height: 1.4;
  color: rgb(255 244 232 / 0.86);
}

.about-line {
  display: block;
  overflow: hidden;
}

.about-stats {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 0.75rem;
  margin-top: 1.15rem;
  padding: 0.9rem 0;
  border-top: 1px solid var(--color-line);
  border-bottom: 1px solid var(--color-line);
}

@media (min-width: 640px) {
  .about-stats {
    grid-template-columns: repeat(4, 1fr);
  }
}

.about-stats b {
  display: block;
  font-family: var(--font-display);
  font-weight: var(--display-weight);
  font-size: clamp(1.4rem, 3vw, 2rem);
  line-height: 1;
  color: var(--color-accent);
}

.about-stats span {
  display: block;
  margin-top: 0.3rem;
  font-weight: 600;
  font-size: 0.6rem;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: rgb(255 244 232 / 0.62);
}

.about-track {
  display: grid;
  grid-template-columns: 1fr;
  gap: clamp(1.1rem, 3vw, 2.5rem);
  margin-top: 1rem;
}

@media (min-width: 860px) {
  .about-track {
    grid-template-columns: 1fr 1fr;
  }
}

.about-h {
  margin: 0 0 0.5rem;
  font-weight: 700;
  font-size: 0.53rem;
  letter-spacing: 0.2em;
  text-transform: uppercase;
  color: var(--color-accent);
}

.about-item {
  border-left: 1px solid var(--color-line);
  padding-left: 0.8rem;
  margin-bottom: 0.65rem;
}

.about-item b {
  display: block;
  font-weight: 600;
  font-size: 0.79rem;
  color: var(--color-paper);
}

.about-item span {
  display: block;
  margin-top: 2px;
  font-weight: 300;
  font-size: 0.69rem;
  color: rgb(255 244 232 / 0.66);
}
```

- [ ] **Step 5: Añadir el gesto de subtitulado**

En `src/themes/vice.choreography.ts`, añadir la función y su llamada:

```ts
/** Gesto 2 — Subtitulado: la ficha entra y las lineas suben encadenadas. */
function scene2Card(gsap: Gsap, root: HTMLElement): void {
  const about = root.querySelector<HTMLElement>('[data-scene="about"]');
  if (!about) return;

  const trigger = { trigger: about, start: "top 78%", toggleActions: "play none none reverse" };
  const card = about.querySelector<HTMLElement>("[data-card]");
  const lines = Array.from(about.querySelectorAll<HTMLElement>("[data-line] > *"));
  const stats = about.querySelector<HTMLElement>("[data-stats]");
  const track = about.querySelector<HTMLElement>("[data-track]");

  if (card) {
    gsap.from(card, { x: -34, opacity: 0, duration: 0.9, ease: "power3.out", scrollTrigger: trigger });
  }
  gsap.from(lines, {
    yPercent: 105,
    opacity: 0,
    duration: 0.85,
    ease: "power3.out",
    stagger: 0.12,
    scrollTrigger: trigger,
  });
  if (stats) {
    gsap.from(stats, { y: 16, opacity: 0, duration: 0.8, ease: "power2.out", delay: 0.35, scrollTrigger: trigger });
  }
  if (track) {
    gsap.from(track, { y: 18, opacity: 0, duration: 0.9, ease: "power2.out", delay: 0.5, scrollTrigger: trigger });
  }
}
```

Y actualizar el export:

```ts
export const viceChoreography: Choreography = ({ gsap, root }) => {
  scene1Title(gsap, root);
  scene2Card(gsap, root);
};
```

- [ ] **Step 6: Verificar**

```bash
export PATH="$HOME/.nvm/versions/node/v22.22.3/bin:$PATH"
npm run build && npm run lint
python3 scripts/verify.py --theme vice
```

Esperado: las cinco aserciones de "Quién es" en OK.

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "feat(vice): ficha de reparto, cifras y gesto de subtitulado

El lado derecho deja de estar vacio: frase de entrada, cifras y trayectoria.
Sin chips de tecnologia, que duplicaban la seccion de creditos."
```

---

## Task 8: Obra — cartela y galería horizontal

**Files:**
- Create: `src/components/gallery.ts`
- Modify: `src/sections/obra/projectScene.ts`, `src/themes/vice.choreography.ts`, `src/style.css`

**Interfaces:**
- Consumes: `GalleryShot` de `src/data/content.ts`.
- Produces: `createGallery(shots: GalleryShot[]): HTMLElement` con `[data-gallery]`, `[data-gallery-track]`, `[data-gallery-bar]`.

- [ ] **Step 1: Añadir la aserción que fallará**

```python
            gal = page.evaluate("""(() => {
              const g = document.querySelector('[data-gallery-track]');
              if (!g) return null;
              return {
                items: g.children.length,
                scrollable: g.scrollWidth > g.clientWidth + 10,
                metas: document.querySelectorAll('[data-scene="obra"] [data-meta]').length,
              };
            })()""")
            check(gal is not None and gal["items"] >= 2, "la galeria tiene piezas")
            check(gal is not None and gal["scrollable"], "la galeria desborda en horizontal")
            check(gal is not None and gal["metas"] >= 1, "las obras tienen fila de metadatos")
```

- [ ] **Step 2: Ejecutar y ver que falla**

```bash
python3 scripts/verify.py --theme vice
```

Esperado: `gal` es `None` → FAIL en las tres.

- [ ] **Step 3: Implementar la galería**

Crear `src/components/gallery.ts`:

```ts
import type { GalleryShot } from "../data/content";
import { el } from "../utils/dom";

/**
 * Galeria horizontal arrastrable. El arrastre se implementa con Pointer
 * Events para que funcione con raton, no solo con trackpad o dedo.
 */
export function createGallery(shots: GalleryShot[]): HTMLElement {
  const track = el(
    "div",
    "gallery-track",
    shots.map((shot) => {
      const image = el("img", "gallery-img");
      image.src = shot.src;
      image.alt = shot.caption;
      image.loading = "lazy";
      image.decoding = "async";

      return el("figure", "gallery-item", [
        image,
        el("figcaption", "gallery-caption", [shot.caption]),
      ]);
    }),
  );
  track.setAttribute("data-gallery-track", "");
  track.tabIndex = 0;
  track.setAttribute("role", "group");
  track.setAttribute("aria-label", "Capturas del proyecto, desplazables en horizontal");

  const bar = el("div", "gallery-bar", [el("i", "", [])]);
  bar.setAttribute("data-gallery-bar", "");

  const gallery = el("div", "gallery", [track, bar]);
  gallery.setAttribute("data-gallery", "");

  let dragging = false;
  let originX = 0;
  let originScroll = 0;

  track.addEventListener("pointerdown", (event: PointerEvent) => {
    dragging = true;
    originX = event.clientX;
    originScroll = track.scrollLeft;
    track.classList.add("is-dragging");
    track.setPointerCapture(event.pointerId);
  });

  track.addEventListener("pointermove", (event: PointerEvent) => {
    if (!dragging) return;
    track.scrollLeft = originScroll - (event.clientX - originX);
  });

  function endDrag() {
    dragging = false;
    track.classList.remove("is-dragging");
  }
  track.addEventListener("pointerup", endDrag);
  track.addEventListener("pointercancel", endDrag);

  const indicator = bar.firstElementChild as HTMLElement;
  function updateBar() {
    const maxScroll = Math.max(1, track.scrollWidth - track.clientWidth);
    const ratio = Math.max(14, (track.clientWidth / track.scrollWidth) * 100);
    indicator.style.width = `${ratio.toFixed(1)}%`;
    indicator.style.marginLeft = `${((track.scrollLeft / maxScroll) * (100 - ratio)).toFixed(1)}%`;
  }
  track.addEventListener("scroll", updateBar, { passive: true });
  updateBar();

  return gallery;
}
```

- [ ] **Step 4: Añadir el CSS de la galería**

En `src/style.css`:

```css
.gallery {
  margin-top: 1rem;
  max-width: 860px;
}

.gallery-track {
  display: flex;
  gap: 0.9rem;
  overflow-x: auto;
  overflow-y: hidden;
  padding-bottom: 0.7rem;
  scroll-snap-type: x proximity;
  cursor: grab;
  scrollbar-width: none;
}

.gallery-track::-webkit-scrollbar {
  display: none;
}

.gallery-track.is-dragging {
  cursor: grabbing;
  scroll-snap-type: none;
}

.gallery-item {
  position: relative;
  flex: 0 0 auto;
  width: min(330px, 76vw);
  margin: 0;
  scroll-snap-align: center;
  border: 1px solid var(--color-line);
  border-radius: var(--radius-card);
  overflow: hidden;
  background: rgb(13 4 24 / 0.6);
  transition:
    border-color 250ms ease,
    transform 250ms ease;
}

.gallery-item:hover {
  border-color: var(--color-accent);
  transform: translateY(-3px);
}

.gallery-img {
  display: block;
  width: 100%;
  aspect-ratio: 16 / 10;
  object-fit: cover;
}

.gallery-caption {
  position: absolute;
  inset: auto 0 0 0;
  padding: 0.35rem 0.6rem;
  background: linear-gradient(0deg, rgb(5 1 10 / 0.94), transparent);
  font-weight: 700;
  font-size: 0.55rem;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: rgb(255 244 232 / 0.9);
}

.gallery-bar {
  height: 2px;
  border-radius: 2px;
  background: rgb(255 244 232 / 0.16);
  overflow: hidden;
}

.gallery-bar i {
  display: block;
  height: 100%;
  background: var(--color-accent);
}

.obra-meta {
  display: flex;
  flex-wrap: wrap;
  gap: 1.4rem;
  margin-top: 0.8rem;
  padding-top: 0.7rem;
  border-top: 1px solid var(--color-line);
  max-width: 860px;
}

.obra-meta dt {
  font-weight: 700;
  font-size: 0.53rem;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: var(--color-accent);
}

.obra-meta dd {
  margin: 0.25rem 0 0;
  font-size: 0.72rem;
  color: rgb(255 244 232 / 0.86);
}
```

- [ ] **Step 5: Reescribir la escena de obra**

En `src/sections/obra/projectScene.ts`, sustituir el cuerpo de `createProjectScene` conservando el import de `el` y añadiendo `createGallery`:

```ts
import { createGallery } from "../../components/gallery";
import type { CaseStudy } from "../../data/content";
import { el } from "../../utils/dom";

function metaEntry(label: string, value: string): HTMLElement {
  return el("div", "", [el("dt", "", [label]), el("dd", "", [value])]);
}

/** Una escena a pantalla completa por proyecto: la obra es la protagonista. */
export function createProjectScene(project: CaseStudy, index: number): HTMLElement {
  const ordinal = el("span", "obra-ord", [String(index + 1).padStart(2, "0")]);
  ordinal.setAttribute("aria-hidden", "true");
  ordinal.setAttribute("data-ord", "");

  const title = el("h2", "display-lg mt-2 max-w-[15ch] text-[clamp(1.5rem,4vw,2.9rem)]", [
    project.title,
  ]);
  title.setAttribute("data-title", "");

  const meta = el("dl", "obra-meta", [
    metaEntry("Rol", project.role),
    ...(project.period ? [metaEntry("Periodo", project.period)] : []),
    metaEntry("Stack", project.stack.slice(0, 3).join(" · ")),
    metaEntry("Estado", project.status),
  ]);
  meta.setAttribute("data-meta", "");

  function column(head: string, text: string): HTMLElement {
    const wrapper = el("div", "overflow-hidden", [
      el("p", "obra-colhead", [head]),
      el("p", "mt-1.5 text-sm leading-relaxed text-paper/85", [text]),
    ]);
    wrapper.setAttribute("data-mask", "");
    return wrapper;
  }

  const columns = el("div", "obra-cols", [
    column("Problema", project.problem),
    column("Solución", project.solution),
  ]);

  const children: HTMLElement[] = [
    ordinal,
    el("p", "hero-kick", [project.tag]),
    title,
    el("p", "lead mt-3 max-w-[32ch] text-paper/85", [project.lead]),
    meta,
    columns,
  ];

  if (project.gallery.length > 0) children.push(createGallery(project.gallery));

  if (project.link) {
    const link = el("a", "obra-link", [project.link.label, " →"]);
    link.href = project.link.href;
    link.target = "_blank";
    link.rel = "noopener noreferrer";
    children.push(link);
  }

  const section = el(
    "section",
    "obra relative flex min-h-screen flex-col justify-center overflow-hidden px-6 py-24 md:px-12",
    children,
  );
  section.setAttribute("data-scene", "obra");
  return section;
}
```

Añadir a `src/style.css`:

```css
.obra-ord {
  position: absolute;
  right: 1.5rem;
  top: 7%;
  font-family: var(--font-display);
  font-weight: var(--display-weight);
  font-size: clamp(3.5rem, 11vw, 8.2rem);
  line-height: 1;
  color: rgb(255 244 232 / 0.1);
  pointer-events: none;
  user-select: none;
}

.obra-cols {
  display: grid;
  grid-template-columns: 1fr;
  gap: clamp(1.1rem, 3vw, 2.6rem);
  margin-top: 0.9rem;
  max-width: 860px;
}

@media (min-width: 760px) {
  .obra-cols {
    grid-template-columns: 1fr 1fr;
  }
}

.obra-colhead {
  font-weight: 700;
  font-size: 0.53rem;
  letter-spacing: 0.2em;
  text-transform: uppercase;
  color: rgb(255 244 232 / 0.5);
}

.obra-link {
  margin-top: 1.4rem;
  align-self: flex-start;
  font-weight: 500;
  font-size: 0.85rem;
  color: var(--color-accent);
  text-decoration: underline;
  text-underline-offset: 7px;
}
```

- [ ] **Step 6: Añadir el gesto de cartela**

En `src/themes/vice.choreography.ts`:

```ts
/** Gesto 3 — Cartela: el ordinal cae, el titulo aterriza seco, el texto barre. */
function scene3Slate(gsap: Gsap, root: HTMLElement): void {
  root.querySelectorAll<HTMLElement>('[data-scene="obra"]').forEach((scene) => {
    const trigger = { trigger: scene, start: "top 76%", toggleActions: "play none none reverse" };
    const ordinal = scene.querySelector<HTMLElement>("[data-ord]");
    const title = scene.querySelector<HTMLElement>("[data-title]");
    const meta = scene.querySelector<HTMLElement>("[data-meta]");
    const masks = Array.from(scene.querySelectorAll<HTMLElement>("[data-mask]"));
    const gallery = scene.querySelector<HTMLElement>("[data-gallery]");

    if (ordinal) {
      gsap.from(ordinal, { y: -70, scale: 1.35, opacity: 0, duration: 0.7, ease: "expo.out", scrollTrigger: trigger });
    }
    if (title) {
      gsap.from(title, { y: 46, opacity: 0, duration: 0.7, ease: "expo.out", delay: 0.08, scrollTrigger: trigger });
    }
    if (meta) {
      gsap.from(meta, { y: 14, opacity: 0, duration: 0.6, ease: "power2.out", delay: 0.2, scrollTrigger: trigger });
    }
    // La mascara barre de izquierda a derecha, desfasada entre columnas.
    gsap.from(masks, {
      clipPath: "inset(0 100% 0 0)",
      duration: 0.9,
      ease: "power3.out",
      stagger: 0.1,
      delay: 0.28,
      scrollTrigger: trigger,
    });
    if (gallery) {
      gsap.from(gallery, { y: 22, opacity: 0, duration: 0.8, ease: "power2.out", delay: 0.44, scrollTrigger: trigger });
    }
  });
}
```

Y en el export:

```ts
export const viceChoreography: Choreography = ({ gsap, root }) => {
  scene1Title(gsap, root);
  scene2Card(gsap, root);
  scene3Slate(gsap, root);
};
```

- [ ] **Step 7: Verificar**

```bash
export PATH="$HOME/.nvm/versions/node/v22.22.3/bin:$PATH"
npm run build && npm run lint
python3 scripts/verify.py --theme vice
```

Esperado: las tres aserciones de galería y metadatos en OK.

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "feat(vice): cartela de obra con metadatos y galeria arrastrable

La galeria usa Pointer Events para funcionar con raton, no solo trackpad."
```

---

## Task 9: Créditos interactivos

**Files:**
- Create: `src/components/credits.ts`
- Modify: `src/sections/skills.ts`, `src/utils/icons.ts`, `src/themes/vice.choreography.ts`, `src/style.css`

**Interfaces:**
- Consumes: `getIconMarkup(slug)` de `src/utils/icons.ts`, `skillGroups` de `content.ts`.
- Produces: `createCredits(): HTMLElement` con `[data-credit][data-index]` y `[data-credit-roll]`.

- [ ] **Step 1: Añadir la aserción que fallará**

```python
            credits = page.evaluate("""(() => {
              const rows = document.querySelectorAll('[data-credit]');
              if (!rows.length) return null;
              const panel = document.querySelector('[data-credit-panel]');
              const before = panel ? panel.textContent.trim() : "";
              rows[rows.length - 1].dispatchEvent(new MouseEvent('mouseenter', {bubbles:true}));
              const after = panel ? panel.textContent.trim() : "";
              return {
                rows: rows.length,
                hasPanel: !!panel,
                hasIcon: !!(panel && panel.querySelector('svg')),
                changed: before !== after,
                focusable: rows[0].tabIndex >= 0,
              };
            })()""")
            check(credits is not None and credits["rows"] >= 6, "hay filas de creditos")
            check(credits is not None and credits["hasPanel"], "hay panel de detalle")
            check(credits is not None and credits["hasIcon"], "el panel muestra icono")
            check(credits is not None and credits["changed"], "el panel cambia al interactuar")
            check(credits is not None and credits["focusable"], "los creditos son enfocables")
```

- [ ] **Step 2: Ejecutar y ver que falla**

```bash
python3 scripts/verify.py --theme vice
```

Esperado: `credits` es `None` → FAIL en las cinco.

- [ ] **Step 3: Implementar el componente**

Crear `src/components/credits.ts`:

```ts
import { skillGroups } from "../data/content";
import { el, elFromMarkup } from "../utils/dom";
import { getIconMarkup } from "../utils/icons";

interface CreditEntry {
  role: string;
  name: string;
  slug: string;
  detail: string;
}

function flatten(): CreditEntry[] {
  return skillGroups.flatMap((group) =>
    group.items.map((item) => ({
      role: group.label,
      name: item.name,
      slug: item.slug,
      detail: item.detail,
    })),
  );
}

/**
 * Creditos de pelicula interactivos: la lista a la izquierda y, a la derecha,
 * el icono real de la tecnologia y para que se usa. El panel nunca arranca
 * vacio. Responde a hover y a foco de teclado, no solo a raton.
 */
export function createCredits(): HTMLElement {
  const entries = flatten();

  const icon = el("div", "credits-icon", []);
  const name = el("p", "credits-panel-name display-lg text-2xl", []);
  const role = el("p", "credits-panel-role", []);
  const detail = el("p", "credits-panel-detail", []);

  const panel = el("div", "credits-panel", [icon, name, role, detail]);
  panel.setAttribute("data-credit-panel", "");
  panel.setAttribute("aria-live", "polite");

  const rows = entries.map((entry, index) => {
    const row = el("button", "credit", [
      el("span", "credit-role", [entry.role]),
      el("span", "credit-name", [entry.name]),
    ]);
    row.type = "button";
    row.setAttribute("data-credit", "");
    row.dataset.index = String(index);

    const select = () => {
      rows.forEach((other) => other.classList.remove("is-active"));
      row.classList.add("is-active");
      icon.replaceChildren(elFromMarkup("credits-svg", getIconMarkup(entry.slug)));
      name.textContent = entry.name;
      role.textContent = entry.role;
      detail.textContent = entry.detail;
    };

    row.addEventListener("mouseenter", select);
    row.addEventListener("focus", select);
    row.addEventListener("click", select);
    return row;
  });

  const list = el("div", "credits-list", rows);
  list.setAttribute("data-credit-roll", "");

  // Estado inicial: el panel muestra la primera entrada.
  rows[0]?.dispatchEvent(new MouseEvent("mouseenter"));

  return el("div", "credits-grid", [list, panel]);
}
```

- [ ] **Step 4: Ampliar el mapa de iconos**

`getIconMarkup` lanza si falta el slug. `skillGroups` usa `react, typescript, tailwindcss, vite, python, django, mysql`, todos ya registrados en `src/utils/icons.ts`. Verificar:

```bash
node -e "
const c=require('fs').readFileSync('src/data/content.ts','utf8');
const i=require('fs').readFileSync('src/utils/icons.ts','utf8');
const slugs=[...c.matchAll(/slug: \"([a-z0-9]+)\"/g)].map(m=>m[1]);
const missing=[...new Set(slugs)].filter(s=>!i.includes('\"'+s+'\"'));
console.log(missing.length?'FALTAN: '+missing.join(', '):'todos los slugs registrados');
"
```

Esperado: `todos los slugs registrados`. Si falta alguno, añadir su import `?raw` y su entrada al `Record` de `src/utils/icons.ts`.

- [ ] **Step 5: Montar la sección y su CSS**

En `src/sections/skills.ts`, sustituir `createSkills` por:

```ts
import { createCredits } from "../components/credits";
import { el } from "../utils/dom";

export function createSkills(): HTMLElement {
  const section = el(
    "section",
    "credits relative flex min-h-screen flex-col justify-center px-6 py-24 md:px-12",
    [el("p", "hero-kick", ["Con qué construyo"]), createCredits()],
  );
  section.setAttribute("data-scene", "credits");
  return section;
}
```

En `src/style.css`:

```css
.credits-grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: clamp(1.4rem, 4vw, 3.4rem);
  align-items: start;
  margin-top: 0.8rem;
  max-width: 940px;
}

@media (min-width: 860px) {
  .credits-grid {
    grid-template-columns: 1fr minmax(230px, 320px);
  }
}

.credit {
  display: flex;
  justify-content: space-between;
  align-items: baseline;
  gap: 1rem;
  width: 100%;
  padding: 0.55rem 0;
  border: 0;
  border-bottom: 1px solid rgb(255 244 232 / 0.16);
  background: none;
  text-align: left;
  cursor: pointer;
  transition: border-color 200ms ease;
}

.credit:hover,
.credit:focus-visible,
.credit.is-active {
  border-color: var(--color-accent);
  outline: none;
}

.credit-role {
  font-weight: 700;
  font-size: 0.6rem;
  letter-spacing: 0.2em;
  text-transform: uppercase;
  color: var(--color-accent);
}

.credit-name {
  font-family: var(--font-display);
  font-weight: var(--display-weight);
  text-transform: var(--display-transform);
  font-size: 1.2rem;
  line-height: 1;
  color: rgb(255 244 232 / 0.62);
  transition: color 200ms ease;
}

.credit:hover .credit-name,
.credit:focus-visible .credit-name,
.credit.is-active .credit-name {
  color: var(--color-paper);
}

.credits-panel {
  border: 1px solid var(--color-line);
  border-radius: var(--radius-card);
  padding: 1.15rem;
  background: rgb(10 2 20 / 0.42);
  backdrop-filter: blur(8px);
}

.credits-svg svg {
  width: 46px;
  height: 46px;
  fill: var(--color-accent);
}

.credits-panel-role {
  margin-top: 0.35rem;
  font-weight: 700;
  font-size: 0.55rem;
  letter-spacing: 0.2em;
  text-transform: uppercase;
  color: var(--color-accent);
}

.credits-panel-detail {
  margin-top: 0.7rem;
  font-weight: 300;
  font-size: 0.79rem;
  line-height: 1.65;
  color: rgb(255 244 232 / 0.82);
}
```

- [ ] **Step 6: Evitar que el nuevo DOM rompa los otros temas**

`createSkills` ahora emite el DOM de créditos para **todos** los temas. Sin estilo
propio, Hyprland y Caelestia mostrarían una lista de créditos, que no es su lenguaje.
El principio del spec es un solo DOM con presentación por tema, así que hay que
darles la suya. En `src/themes/themes.css`:

```css
/* Hyprland: el mismo DOM se lee como pildoras en rejilla, no como creditos. */
:root[data-theme="hyprland"] .credits-grid,
:root[data-theme="caelestia"] .credits-grid {
  grid-template-columns: 1fr;
}

:root[data-theme="hyprland"] .credits-list,
:root[data-theme="caelestia"] .credits-list {
  display: flex;
  flex-wrap: wrap;
  gap: 0.5rem;
}

:root[data-theme="hyprland"] .credit,
:root[data-theme="caelestia"] .credit {
  width: auto;
  justify-content: flex-start;
  gap: 0.5rem;
  padding: 0.4rem 0.75rem;
  border: 1px solid var(--color-line);
  border-radius: var(--radius-card);
}

/* El rol sobra en formato pildora: manda el nombre. */
:root[data-theme="hyprland"] .credit-role,
:root[data-theme="caelestia"] .credit-role {
  display: none;
}

:root[data-theme="hyprland"] .credit-name,
:root[data-theme="caelestia"] .credit-name {
  font-family: var(--font-body);
  font-size: 0.8rem;
  text-transform: none;
  color: var(--color-paper);
}
```

Verificar que ningún tema queda roto:

```bash
python3 scripts/verify.py --theme hyprland
python3 scripts/verify.py --theme caelestia
```

Esperado: `TODO OK` en ambos.

- [ ] **Step 7: Añadir el gesto de créditos**

En `src/themes/vice.choreography.ts`:

```ts
/** Gesto 4 — Creditos: ruedan al entrar y SE DETIENEN. Un rodillo perpetuo
 *  seria imposible de usar, y esta seccion es interactiva. */
function scene4Credits(gsap: Gsap, root: HTMLElement): void {
  const roll = root.querySelector<HTMLElement>("[data-credit-roll]");
  if (!roll) return;

  gsap.from(roll.children, {
    y: 34,
    opacity: 0,
    duration: 0.7,
    ease: "power3.out",
    stagger: 0.07,
    scrollTrigger: { trigger: roll, start: "top 80%", toggleActions: "play none none reverse" },
  });
}
```

Y en el export:

```ts
export const viceChoreography: Choreography = ({ gsap, root }) => {
  scene1Title(gsap, root);
  scene2Card(gsap, root);
  scene3Slate(gsap, root);
  scene4Credits(gsap, root);
};
```

- [ ] **Step 8: Verificar**

```bash
export PATH="$HOME/.nvm/versions/node/v22.22.3/bin:$PATH"
npm run build && npm run lint
python3 scripts/verify.py --theme vice
```

Esperado: las cinco aserciones de créditos en OK.

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "feat(vice): creditos interactivos con iconos y panel de detalle

Los creditos ruedan al entrar y se detienen: la seccion es interactiva.
Cada fila es un boton, asi que responde tambien a foco de teclado."
```

---

## Task 10: Contacto, letterbox y barra de orientación

**Files:**
- Create: `src/sections/contacto.ts`, `src/components/cinemaChrome.ts`
- Modify: `src/main.ts`, `src/themes/vice.choreography.ts`, `src/style.css`

**Interfaces:**
- Produces: `createContacto(): HTMLElement` con `data-scene="contacto"`; `createCinemaChrome(): HTMLElement` con `[data-letterbox]` y `[data-rail]`.

- [ ] **Step 1: Añadir la aserción que fallará**

```python
            chrome = page.evaluate("""(() => ({
              contacto: !!document.querySelector('[data-scene="contacto"]'),
              letterbox: document.querySelectorAll('[data-letterbox]').length,
              rail: !!document.querySelector('[data-rail]'),
            }))()""")
            check(chrome["contacto"], "existe la seccion de contacto")
            check(chrome["letterbox"] == 2, "hay dos barras de letterbox")
            check(chrome["rail"], "existe la barra de orientacion")

            # Solo hero y contacto van a plena luz; las intermedias se atenuan.
            page.evaluate("window.scrollTo(0, document.body.scrollHeight * 0.45)")
            page.wait_for_timeout(1200)
            dimmed = page.evaluate(
                "parseFloat(getComputedStyle(document.querySelector('[data-dim]')).opacity)"
            )
            check(dimmed > 0.3, f"el fondo se atenua en secciones interiores ({dimmed})")
```

- [ ] **Step 2: Ejecutar y ver que falla**

```bash
python3 scripts/verify.py --theme vice
```

Esperado: FAIL en las tres.

- [ ] **Step 3: Crear la sección de contacto**

Crear `src/sections/contacto.ts`:

```ts
import { identity } from "../data/content";
import { el } from "../utils/dom";

export function createContacto(): HTMLElement {
  const email = el("a", "contacto-mail", [identity.email]);
  email.href = `mailto:${identity.email}`;

  const github = el("a", "", ["GitHub"]);
  github.href = identity.github;
  github.target = "_blank";
  github.rel = "noopener noreferrer";

  const section = el(
    "section",
    "contacto relative flex min-h-screen flex-col items-center justify-center px-6 py-24 text-center md:px-12",
    [
      el("p", "hero-kick", ["Contacto"]),
      el("h2", "display-xl mt-3 text-[clamp(2rem,6vw,4rem)]", ["Hablemos"]),
      el("p", "mt-6", [email]),
      el("div", "hero-corner", [
        el("span", "", [identity.phone]),
        github,
      ]),
    ],
  );
  section.setAttribute("data-scene", "contacto");
  return section;
}
```

En `src/style.css`:

```css
.contacto-mail {
  font-size: clamp(1rem, 2.2vw, 1.4rem);
  color: var(--color-accent);
  text-decoration: underline;
  text-underline-offset: 8px;
  text-decoration-thickness: 2px;
}
```

- [ ] **Step 4: Crear el cromo de cine**

Crear `src/components/cinemaChrome.ts`:

```ts
import { identity } from "../data/content";
import { el } from "../utils/dom";

/**
 * Recursos globales de lenguaje de cine: barras de letterbox (entran solo en
 * la obra) y barra de orientacion, que evita perderse en un recorrido largo.
 */
export function createCinemaChrome(): HTMLElement {
  const top = el("div", "letterbox letterbox-top", []);
  top.setAttribute("data-letterbox", "");
  const bottom = el("div", "letterbox letterbox-bottom", []);
  bottom.setAttribute("data-letterbox", "");

  const rail = el("div", "rail", [
    el("span", "", [identity.name]),
    el("span", "rail-now", ["01 · Título"]),
  ]);
  rail.setAttribute("data-rail", "");
  rail.setAttribute("aria-hidden", "true");

  // Atenuador del fondo: solo hero y contacto van a plena luz. Con todas las
  // secciones a imagen brillante, el texto largo pierde legibilidad y el
  // recorrido cansa.
  const dim = el("div", "backdrop-dim", []);
  dim.setAttribute("data-dim", "");

  const chrome = el("div", "cinema-chrome", [dim, top, bottom, rail]);
  chrome.setAttribute("aria-hidden", "true");
  return chrome;
}
```

Y su CSS en `src/style.css`:

```css
.backdrop-dim {
  position: fixed;
  inset: 0;
  z-index: -5;
  pointer-events: none;
  background: #05010a;
  opacity: 0;
}
```

En `src/style.css`:

```css
.cinema-chrome {
  pointer-events: none;
}

.letterbox {
  position: fixed;
  left: 0;
  right: 0;
  height: 0;
  z-index: 40;
  background: #05010a;
  transition: height 450ms cubic-bezier(0.16, 1, 0.3, 1);
}

.letterbox-top { top: 0; }
.letterbox-bottom { bottom: 0; }

.rail {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 41;
  display: flex;
  justify-content: space-between;
  padding: 0.75rem 1.5rem;
  font-weight: 700;
  font-size: 0.58rem;
  letter-spacing: 0.24em;
  text-transform: uppercase;
  color: rgb(255 244 232 / 0.6);
}

@media (min-width: 768px) {
  .rail {
    padding-inline: 3rem;
  }
}

.rail-now {
  color: var(--color-accent);
}
```

- [ ] **Step 5: Componer en main.ts**

En `src/main.ts`, sustituir la construcción de `main` y el `append`:

```ts
import { createAbout } from "./sections/about";
import { createContacto } from "./sections/contacto";
import { createCinemaChrome } from "./components/cinemaChrome";
import { createSkills } from "./sections/skills";

const main = el("main", "relative", [
  createHero(),
  createAbout(),
  ...caseStudies.map((project, index) => createProjectScene(project, index)),
  createSkills(),
  createContacto(),
]);

app.append(
  backgroundHost,
  noise,
  main,
  createCinemaChrome(),
  createThemeSignature(theme.label),
);
```

- [ ] **Step 6: Cablear letterbox y barra en la coreografía**

En `src/themes/vice.choreography.ts`:

```ts
/** Recursos globales: letterbox solo en la obra y barra de orientacion viva. */
function cinemaChrome(gsap: Gsap, root: HTMLElement, ScrollTrigger: ScrollTriggerApi): void {
  const bars = Array.from(document.querySelectorAll<HTMLElement>("[data-letterbox]"));
  const now = document.querySelector<HTMLElement>(".rail-now");
  const dim = document.querySelector<HTMLElement>("[data-dim]");
  const scenes = Array.from(root.querySelectorAll<HTMLElement>("[data-scene]"));
  const labels: Record<string, string> = {
    hero: "Título",
    about: "Ficha",
    obra: "Cartela",
    credits: "Créditos",
    contacto: "Fundido",
  };
  /** Solo hero y contacto a plena luz; el resto atenuado para que el texto lea. */
  const brightScenes = new Set(["hero", "contacto"]);

  scenes.forEach((scene, index) => {
    const kind = scene.dataset.scene ?? "";
    ScrollTrigger.create({
      trigger: scene,
      start: "top 50%",
      end: "bottom 50%",
      onToggle: (self) => {
        if (!self.isActive) return;
        if (now) {
          now.textContent = `${String(index + 1).padStart(2, "0")} · ${labels[kind] ?? ""}`;
        }
        // Las franjas entran solo durante la obra: dicen "esto es la pelicula".
        gsap.to(bars, { height: kind === "obra" ? "6.5vh" : "0vh", duration: 0.45, ease: "power3.out" });
        if (dim) {
          gsap.to(dim, {
            opacity: brightScenes.has(kind) ? 0 : 0.62,
            duration: 0.6,
            ease: "power2.out",
          });
        }
      },
    });
  });
}
```

Actualizar el export y su firma:

```ts
export const viceChoreography: Choreography = ({ gsap, root, ScrollTrigger }) => {
  scene1Title(gsap, root);
  scene2Card(gsap, root);
  scene3Slate(gsap, root);
  scene4Credits(gsap, root);
  cinemaChrome(gsap, root, ScrollTrigger);
};
```

Añadir el import de tipo al principio del archivo:

```ts
import type { Choreography, Gsap, ScrollTriggerApi } from "./choreography";
```

- [ ] **Step 7: Verificar**

```bash
export PATH="$HOME/.nvm/versions/node/v22.22.3/bin:$PATH"
npm run build && npm run lint
python3 scripts/verify.py --theme vice
python3 scripts/verify.py --theme hyprland
```

Esperado: todas las aserciones de Vice en OK y Hyprland sin regresión.

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "feat(vice): contacto, letterbox y barra de orientacion

Las franjas entran solo durante la obra; la barra indica seccion y numero."
```

---

## Task 11: Assets reales y optimización

**Files:**
- Create: `public/media/*`
- Modify: `index.html`, `src/data/content.ts` (si cambian rutas)

**Interfaces:**
- Consumes: rutas declaradas en la Task 2 y la Task 3.

- [ ] **Step 1: Añadir la aserción de peso que fallará**

```python
            weights = page.evaluate("""(() => {
              const e = performance.getEntriesByType('resource')
                .filter(r => r.name.includes('/media/'));
              const total = e.reduce((sum, r) => sum + (r.encodedBodySize || 0), 0);
              return { count: e.length, kb: Math.round(total / 1024) };
            })()""")
            check(weights["kb"] < 1800, f"los media pesan menos de 1,8 MB (son {weights['kb']} kB)")
```

- [ ] **Step 2: Ejecutar y ver que falla**

```bash
python3 scripts/verify.py --theme vice
```

Esperado: FAIL — el mp4 sin optimizar de la Task 3 pesa ~3,1 MB.

- [ ] **Step 3: Recortar y re-codificar el vídeo**

Requiere `ffmpeg`. Si no está instalado, pedirlo al usuario (es dependencia de sistema, no de proyecto).

```bash
cd /home/aoshi/proyectos/portfolio-aoshi
# Recorte de 7 s con punto de bucle limpio y sin audio
ffmpeg -y -ss 2 -t 7 -i public/media/vice-hero.mp4 -an \
  -vf "scale=1280:-2" -c:v libx264 -preset slow -crf 30 -movflags +faststart \
  public/media/vice-hero.opt.mp4
ffmpeg -y -ss 2 -t 7 -i public/media/vice-hero.mp4 -an \
  -vf "scale=1280:-2" -c:v libvpx-vp9 -b:v 0 -crf 40 -row-mt 1 \
  public/media/vice-hero.opt.webm
mv public/media/vice-hero.opt.mp4 public/media/vice-hero.mp4
mv public/media/vice-hero.opt.webm public/media/vice-hero.webm
ls -la public/media/
```

Esperado: ambos por debajo de ~1,3 MB. El grano y la gradación van por encima, así que aguanta un CRF alto sin que se note.

- [ ] **Step 4: Añadir las capturas reales**

Aoshi aporta las capturas. Convertir a WebP y colocarlas con los nombres que declara `content.ts`:

```bash
mkdir -p public/media/obra
# Por cada captura entregada (ejemplo con una):
ffmpeg -y -i /ruta/a/captura-original.png -vf "scale=1280:-2" -quality 82 \
  public/media/obra/campaign-panel.webp
ls public/media/obra/
```

Los nombres deben coincidir exactamente con los de `src/data/content.ts` (Task 2). Si alguna captura del proyecto de Telefónica no puede publicarse, sustituirla por un diagrama de arquitectura y **actualizar su `caption`** para no inducir a error.

- [ ] **Step 5: Precargar el póster**

En `index.html`, tras los `preconnect`:

```html
    <link rel="preload" as="image" href="/media/vice-poster.webp" fetchpriority="high" />
```

- [ ] **Step 6: Verificar**

```bash
export PATH="$HOME/.nvm/versions/node/v22.22.3/bin:$PATH"
npm run build && npm run lint
python3 scripts/verify.py --theme vice
```

Esperado: la aserción de peso en OK, sin imágenes rotas en consola.

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "perf(vice): video recortado y capturas reales optimizadas

Recorte de 7 s con bucle limpio en WebM y MP4 a 1280x720. El grano y la
gradacion permiten un CRF alto sin perdida visible."
```

---

## Task 12: Degradación, accesibilidad y gate visual

**Files:**
- Modify: `scripts/verify.py`, `src/style.css`

- [ ] **Step 1: Añadir el modo de degradación al arnés**

En `scripts/verify.py`, añadir a `run()` un parámetro y un segundo contexto. Sustituir la firma por `def run(theme: str, url: str, reduced: bool = False) -> None:` y la creación de la página por:

```python
        page = browser.new_page(
            viewport=DESKTOP,
            reduced_motion="reduce" if reduced else "no-preference",
        )
```

Añadir al final de `run()`, antes de `page.close()`:

```python
        if reduced:
            degraded = page.evaluate("""(() => {
              const host = document.querySelector('.bg-theme');
              const bars = [...document.querySelectorAll('[data-letterbox]')];
              return {
                noVideo: !host || !host.querySelector('video'),
                barsClosed: bars.every(b => b.getBoundingClientRect().height < 1),
                galleryUsable: !!document.querySelector('[data-gallery-track]'),
                creditsUsable: document.querySelectorAll('[data-credit]').length > 0,
                textVisible: (() => {
                  const n = document.querySelector('[data-hero-name]');
                  return !!n && getComputedStyle(n).opacity === "1";
                })(),
              };
            })()""")
            check(degraded["noVideo"], "sin video con reduced-motion")
            check(degraded["barsClosed"], "sin letterbox con reduced-motion")
            check(degraded["textVisible"], "el nombre es legible con reduced-motion")
            # Son contenido, no decoracion: apagarlos dejaria sin informacion.
            check(degraded["galleryUsable"], "la galeria sigue disponible")
            check(degraded["creditsUsable"], "los creditos siguen disponibles")
```

Y en `main()`:

```python
    ap.add_argument("--reduced", action="store_true")
    args = ap.parse_args()
    run(args.theme, args.url, args.reduced)
```

- [ ] **Step 2: Ejecutar y ver qué falla**

```bash
python3 scripts/verify.py --theme vice --reduced
```

Esperado: FAIL en `el nombre es legible` — la clase `js-intro` lo oculta y la coreografía, que es quien la retira, no corre con reduced-motion.

- [ ] **Step 3: Corregir la degradación**

En `src/main.ts`, la clase `js-intro` ya solo se añade si no hay reduced-motion. Verificar que la condición sigue intacta y que el `setTimeout` de seguridad permanece. En `src/style.css`, blindar el caso:

```css
/*
 * Si el usuario pide movimiento reducido, el contenido nunca debe quedar
 * oculto esperando una animacion que no va a ejecutarse.
 */
@media (prefers-reduced-motion: reduce) {
  :root.js-intro [data-hero-name],
  :root.js-intro [data-hero-fade] {
    opacity: 1;
  }

  .letterbox {
    display: none;
  }
}
```

- [ ] **Step 4: Verificar la matriz completa**

```bash
export PATH="$HOME/.nvm/versions/node/v22.22.3/bin:$PATH"
npm run build && npm run lint
for t in vice hyprland caelestia; do python3 scripts/verify.py --theme "$t"; done
python3 scripts/verify.py --theme vice --reduced
grep -rE "lorem|placeholder|mockData|TODO.*real" src/ || echo "anti-mock limpio"
```

Esperado: `TODO OK` en los cuatro pases y `anti-mock limpio`.

- [ ] **Step 5: Capturas para el gate visual**

```bash
python3 - <<'PY'
from playwright.sync_api import sync_playwright
OUT="/tmp/vice-gate"
import os; os.makedirs(OUT, exist_ok=True)
with sync_playwright() as p:
    b=p.chromium.launch(headless=True, executable_path="/usr/bin/google-chrome",
        args=["--no-sandbox","--use-gl=swiftshader","--enable-unsafe-swiftshader"])
    for vp,tag in [({"width":1440,"height":900},"desktop"),({"width":390,"height":844},"mobile")]:
        pg=b.new_page(viewport=vp)
        pg.goto("http://127.0.0.1:5173/?theme=vice", wait_until="domcontentloaded", timeout=30000)
        pg.wait_for_timeout(4000)
        pg.screenshot(path=f"{OUT}/vice-{tag}-hero.png")
        for y,name in [(1400,"about"),(2600,"obra"),(4200,"credits")]:
            pg.evaluate(f"window.scrollTo(0,{y})"); pg.wait_for_timeout(1500)
            pg.screenshot(path=f"{OUT}/vice-{tag}-{name}.png")
        pg.close()
    b.close()
print("capturas en", OUT)
PY
```

- [ ] **Step 6: Gate humano**

Enviar las capturas a Aoshi y **esperar aprobación** antes de considerar la tarea cerrada. Si aprueba, ejecutar los subagentes críticos del proyecto (`lidia-naive-tester` para flujo y `vera-art-director` para ejecución visual) antes de plantear el merge a `main`.

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "feat(vice): degradacion accesible y matriz de verificacion

Con reduced-motion se apagan video, letterbox y animaciones, pero la galeria
y los creditos siguen operativos: son contenido, no decoracion."
```

---

## Notas de ejecución

- **El shader de Vice desaparece**, pero `src/backgrounds/shaderBackground.ts` **se queda**: Hyprland y Caelestia lo siguen usando. No borrarlo.
- **`src/components/caseStudyPanel.ts` y `src/sections/caseStudies.ts`** quedan sin importadores tras la Task 10. Retirarlos en esa tarea con `git rm` si `grep -rn "caseStudyPanel\|createCaseStudies" src/` no devuelve nada.
- **`CLAUDE.md` menciona Three.js en el stack** y deja de ser cierto en la Task 1. No editarlo durante la sesión (regla del proyecto: invalida el prompt cache); anotarlo y corregirlo al cerrar.
- Si `ffmpeg` no está disponible, la Task 11 se bloquea: es dependencia de sistema y requiere aprobación del usuario para instalarla.
